export const metadata = { title: 'WAYtoAGI 学习路径 — Study Reviver' }
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="zh-CN"><body style={{background:'#f5f5f5',margin:0}}>{children}</body></html>
}
